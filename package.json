{
  "name": "auto-close-org-issues-action",
  "version": "1.0.0",
  "description": "Automatically close issues that are raised by people that belong to a specific organization using GitHub Actions",
  "main": "dist/index.js",
  "scripts": {
    "test": "jest",
    "build": "npx @vercel/ncc build && npx convert-action"
  },
  "keywords": [],
  "author": "Michael Heap <m@michaelheap.com>",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "mocked-env": "^1.3.5",
    "nock": "^13.4.0"
  }
}
